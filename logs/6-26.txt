npc dogs
-> first the customers, then the waiters 
TODO:
    -> dog building 
    -> dog factory to schedule and automate building
    -> define coords for the queue

factory builds the dog
factory is owned by the level

level emits the build dog request when 
that way the factory can have the requested id 

but how does it insert the dog into the level, the 
level needs to manage that

so is the path game -> level, level builds 
i think so
the level will be a factory itself

simples, then the level should listen for build dog events to be emitted
how often is the question, 

listen for hte cusomter_arrives event. doesn't have data, just emits .
it would be a bit odd for the matire_d to handle this , but in a way it would make sense because the matire d owns the customer queue 
it means that the matire d would need to have an update loop which it should already have and will definitely need at some point anyway if it doesnt have it yet. 

for now i think we can jsut have customer arrive of a keyt press\, say l, can just wire it in a simple if branch in the game since its just debugging, add a 
comment header and footer // debug beahvuiours in the game update loop, this check should exist there 

in addition to the real logic which we can discuss and tweak for now it could look like 
-> if the queue is not full
    -> if a dog hasnt arrived in the last 2 minuts
    -> or if at least 3 dogs have left in the last 30 seconds

each frame check would look like 
    if can_add_dog then 
        emit build event 
    update frames_since_dog_arrived -> (like a frames ++ increment)
    update num_dogs_left_in_last_thirty_seconds (if (thirty_sceond_counter === config::THIRTY_SECONDS) then num_dogs = 0)
    
then the matire d listens for dog leave and dog arrive events to update counteres
those events i dont think would need to carry any data 


define the customer queue space

for debugging purposes for now we should  the perimeter of the quesu sapce with raylibs drawLine, here add a debug section for level::render adn then draw this queue in
I think it should take 10 % of the screen width, and the whole height which i think is (1920 * 108).

then we define the constants for hte queue dog positions, at least the baseline part before potential dynamic validation

again we discussed about possitions being malleable so this should also be behaviour that the matire_d handles in its update loop. unless this approach is messy and kind 
of complicated for not that much gain, like perhaps its best to just define each position being like 3 * edge weights appart and then not making a dog taller than that, 



1 yes, 
2 no build dog event doesnt need id, level will supply
3 yes its redundandt
4 of the screen
5 i think static, contant positions is just way easier for us to work with. i think we can have more posisiotns ,what ever the floor of  screen height - (10 * edge weights) / (3 * edge weifghts) is. 
I would give a 5 edge buffer on either side for now, define these coefficients as constants too 
6 float delta input to match other updates, i think is time since last frame unsure what hte unit of time is, is it  ms or  s. otherwise if frame is already wired throug hthe game then yeah use that  
7 have it working for now, gate it befind a config flag though, new config namespcae, called feature_flag0config, and have it include bool automatic_arrivals = true. amend the ocndition to say if the queue is empoty, build a dog, otherwise we 
would never have any dogs, yk , like we need at least 1 dog otherwise the system never starts


i dont like functions in the config, they should be variables, it should be maitre_d::update(flaot delta){
    - perform logic for checking customer arrival, 
    - then call porocess events
}



ok and the debugging event render tool (like a logger, this is hwo you build it)
like a visual debug tool, yeah let's build thjat   

very simple tool 



ok so we've setup the queue and dog building
rfemember this checklist for the rest of this feature so next is 
-> creating the dog build key input command
-> checking the npc dog pathfinding (it should already exist in the player dog logic right ) ? 
-> wiring up queue joining events for the matire d to actually give queue positions for path destinations
-> then testing queue joining behaviour 
-> then table setup 
-> then table pathinding 