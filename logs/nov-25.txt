-> index calculation of the final perimeter node index is off, 
the final node in each row


-> fix the cursor bug 
-> then implement the dogs
-> then implement cursor interaction and collision
-> then implement the controls for the dogs (path selection, dog selection)

-> double trouble with the cursors yo, one is the player and one is the entity i think ?

remove it from player
WHEN THINGS GET COPY CONSTRUCTED THEY LOSE THEIR SUBSCRIPTION TO THE EVENT 
THYE NO LONGER LISTEN


the cursor needs to listen to the left mouse down events so the numbers can change properly
-> copy over the event handlers 
-> and add debug statements


the events thing is working well
still unsure as to why things are freezing, it definitely is a moving entities in the tree thing, need to test more
i think some adjustments should be made to the tree, how it is handled, how it manages entities, how children are created, etc.

change th eoctree to buid all children when a node is created and to remove and reinsert from the root when moved

do i prefer the idea of the move and remove entity as events that the tree listens to
both remove the entity, move reinserts it

might be better ? 
collisions are checked every frame still though

-> slight out of bounds issue with the cursor, it can go beyond, so clamp it ? 

-> dogs and entitiy interaction 
    -> dogs class
    -> dog builders 
    -> dog art
    -> dog cursor controls
        -> dog selection (and highlighting the selected one )
        -> move to point
    -> cursor interactions

let's break it down 
first, build the dogs
second, be able to select the dogs, comprised of cursor interactions and prompts

ok let's do that 
     


regarding interaction, start with a simple 
if else dynamic cast thing for now, will work on a 
more elegant solution later on

consider a way for it to be turned off, i.e while interacting do this, while not do that

and a way to select items

ok kind of crazy idea, but dont query interactions every frame, rather interpret interactions as an event
that could be created when an entity moves, because interactions can only occur when an entity moves into another


-> implement queries
-> adjust handler comparisons for events and queries
-> setup cursor queries

-> if I can template the handler over a query type, can i tempalte it over the return type too ? 